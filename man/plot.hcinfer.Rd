% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.hcinfer}
\alias{plot.hcinfer}
\title{Plot robust confidence intervals}
\usage{
\method{plot}{hcinfer}(x, parm, ...)
}
\arguments{
\item{x}{An object returned by \code{\link[=hcinfer]{hcinfer()}}.}

\item{parm}{Optional coefficient names or integer positions. When supplied,
only the selected coefficients are plotted. The selection follows the same
rules as \code{\link[=confint.hcinfer]{confint.hcinfer()}} and \code{\link[=tests.hcinfer]{tests.hcinfer()}}.}

\item{...}{Unused. Passing named arguments raises an error.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Plots normal Wald confidence intervals for an \code{\link[=hcinfer]{hcinfer()}} result. Each
interval is color-coded by the test decision at the stored significance
level: coefficients for which the null hypothesis is rejected are shown in
red, and those for which it is not rejected are shown in blue. Formatted
p-values are printed to the right of each interval for quick reading.
}
\examples{
schools <- PublicSchools |>
  dplyr::mutate(
    income_scaled = income / 10000,
    income_scaled_sq = income_scaled^2
  )
fit <- lm(expenditure ~ income_scaled + income_scaled_sq, data = schools)
result <- hcinfer(fit)
plot(result)
plot(result, parm = "income_scaled_sq")

}
\seealso{
\code{\link[=hcinfer]{hcinfer()}}, \code{\link[=confint.hcinfer]{confint.hcinfer()}}, \code{\link[=tests.hcinfer]{tests.hcinfer()}}
}
