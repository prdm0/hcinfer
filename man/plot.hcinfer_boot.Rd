% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.hcinfer_boot}
\alias{plot.hcinfer_boot}
\title{Plot pairs bootstrap confidence intervals}
\usage{
\method{plot}{hcinfer_boot}(x, parm, ...)
}
\arguments{
\item{x}{An object returned by \code{\link[=boot_pairs]{boot_pairs()}}.}

\item{parm}{Optional coefficient names or integer positions. When supplied,
only the selected coefficients are plotted, following the same rules as
\code{\link[=confint.hcinfer_boot]{confint.hcinfer_boot()}}.}

\item{...}{Unused. Passing named arguments raises an error.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Plots the pairs bootstrap confidence intervals stored in a \code{\link[=boot_pairs]{boot_pairs()}}
object. Each coefficient is drawn as its ordinary least squares point estimate
with a horizontal bootstrap interval, color-coded by whether the interval
excludes zero (shown in red) or includes zero (shown in blue). A dashed
vertical reference line is drawn at zero.
}
\examples{
schools <- PublicSchools |>
  dplyr::mutate(
    income_scaled = income / 10000,
    income_scaled_sq = income_scaled^2
  )
fit <- lm(expenditure ~ income_scaled + income_scaled_sq, data = schools)
boot <- boot_pairs(fit, B = 1000, seed = 123)
plot(boot)
plot(boot, parm = "income_scaled_sq")

}
\seealso{
\code{\link[=boot_pairs]{boot_pairs()}}, \code{\link[=plot.hcinfer]{plot.hcinfer()}}
}
