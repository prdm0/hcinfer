% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.hcinfer_vcov}
\alias{plot.hcinfer_vcov}
\title{Plot HC adjustment factors against leverages}
\usage{
\method{plot}{hcinfer_vcov}(x, label_top = 3, ...)
}
\arguments{
\item{x}{An object returned by \code{\link[=vcov_hc]{vcov_hc()}}.}

\item{label_top}{A nonnegative whole number. The observations with the
largest adjustment factors are labeled. Use \code{0} to suppress labels.}

\item{...}{Unused. Passing named arguments raises an error.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Plots the HC adjustment factors \eqn{g_t} against the leverage values
\eqn{h_t} stored in a \code{\link[=vcov_hc]{vcov_hc()}} object. Points with
\eqn{h_t > 3p/n} are highlighted because this threshold is commonly used to
flag high-leverage observations in the empirical examples from the HCbeta
paper.
}
\examples{
schools <- PublicSchools |>
  dplyr::mutate(
    income_scaled = income / 10000,
    income_scaled_sq = income_scaled^2
  )
fit <- lm(expenditure ~ income_scaled + income_scaled_sq, data = schools)

cov <- vcov_hc(fit, type = "hcbeta")
plot(cov)
plot(vcov_hc(fit, type = "hc4"), label_top = 2)

}
\seealso{
\code{\link[=vcov_hc]{vcov_hc()}}, \code{\link[=hcinfer]{hcinfer()}}, \code{\link[=plot.hcinfer]{plot.hcinfer()}}
}
