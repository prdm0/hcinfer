% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov-hc.R
\name{vcov_hc}
\alias{vcov_hc}
\title{Heteroskedasticity-consistent covariance estimator}
\usage{
vcov_hc(object, type = "hcbeta", ...)
}
\arguments{
\item{object}{An ordinary least squares model fitted by \code{\link[stats:lm]{stats::lm()}}.}

\item{type}{A character string specifying the HC estimator. The default is
\code{"hcbeta"}.}

\item{...}{Method-specific constants. Unknown names are rejected. See Details
for the accepted names, defaults, and parameter domains.}
}
\value{
An object of class \code{hcinfer_vcov}. The covariance matrix is stored in
\code{object$vcov} and is returned directly by \code{\link[=vcov]{vcov()}}.
}
\description{
Computes a heteroskedasticity-consistent covariance matrix estimator for an
ordinary least squares model fitted with \code{\link[stats:lm]{stats::lm()}}. The function returns
a rich S3 object that stores the covariance matrix, HC weights, leverage
values, method parameters, and model metadata.
}
\details{
For a linear model with design matrix \eqn{X}, OLS residuals \eqn{\hat e_t},
and HC weights \eqn{g_t}, the estimator is

\deqn{\widehat{\Psi}_{HC} =
(X'X)^{-1} X' \widehat{\Omega} X (X'X)^{-1},}

where \eqn{\widehat{\Omega} = diag(\hat e_t^2 g_t)}. The supported
estimators are \code{"hc0"}, \code{"hc1"}, \code{"hc2"}, \code{"hc3"}, \code{"hc4"}, \code{"hc4m"},
\code{"hc5"}, \code{"hc5m"}, and \code{"hcbeta"}.

Additional arguments in \code{...} are method-specific. The defaults are:
\itemize{
\item \code{"hc0"}, \code{"hc1"}, \code{"hc2"}, \code{"hc3"}, \code{"hc4"}, and \code{"hc4m"}: no
method-specific arguments.
\item \code{"hc5"}: \code{k = 0.7}.
\item \code{"hc5m"}: \code{k = 0.7}, \code{k1 = 1}, \code{k2 = 0}, \code{k3 = 1}, \code{gamma1 = 1}, and
\code{gamma2 = 1.5}.
\item \code{"hcbeta"}: \code{c1 = 7}, \code{c2 = 0.75}, \code{lower = 0.01}, and \code{upper = 0.99}.
}

For \code{"hc5"} and \code{"hc5m"}, \code{k}, \code{k1}, \code{k2}, and \code{k3} must be nonnegative,
while \code{gamma1} and \code{gamma2} must be positive. For \code{"hcbeta"}, \code{c1} must be
nonnegative, \code{c2} must be positive, and \code{lower} and \code{upper} must lie in
\verb{(0, 1)} with \code{lower < upper}. The HCbeta truncation is
\eqn{w_t = max(lower, min(1 - h_t, upper))}.
}
\examples{
schools <- PublicSchools |>
  dplyr::mutate(
    income_scaled = income / 10000,
    income_scaled_sq = income_scaled^2
  )
fit <- lm(expenditure ~ income_scaled + income_scaled_sq, data = schools)
cov <- vcov_hc(fit, type = "hcbeta")
cov
vcov(cov)
plot(cov)

vcov_hc(fit, type = "hcbeta", c1 = 7, c2 = 0.75, lower = 0.01, upper = 0.99)
vcov_hc(fit, type = "hc5", k = 0.7)
vcov_hc(fit, type = "hc5m", k = 0.7, k1 = 1, k2 = 0, k3 = 1)

}
\references{
White, H. (1980). A heteroskedasticity-consistent covariance matrix estimator
and a direct test for heteroskedasticity. \emph{Econometrica}, 48(4), 817-838.
\doi{10.2307/1912934}

Hinkley, D. V. (1977). Jackknifing in unbalanced situations.
\emph{Technometrics}, 19(3), 285-292.
\doi{10.1080/00401706.1977.10489550}

MacKinnon, J. G. and White, H. (1985). Some heteroskedasticity-consistent
covariance matrix estimators with improved finite sample properties.
\emph{Journal of Econometrics}, 29(3), 305-325.
\doi{10.1016/0304-4076(85)90158-7}

Davidson, R. and MacKinnon, J. G. (1993). \emph{Estimation and Inference in
Econometrics}. Oxford University Press.

Cribari-Neto, F. (2004). Asymptotic inference under heteroskedasticity of
unknown form. \emph{Computational Statistics and Data Analysis}, 45(2), 215-233.
\doi{10.1016/S0167-9473(02)00366-3}

Cribari-Neto, F. and da Silva, W. B. (2011). A new heteroskedasticity
consistent covariance matrix estimator for the linear regression model.
\emph{AStA Advances in Statistical Analysis}, 95(2), 129-146.
\doi{10.1007/s10182-010-0141-2}

Cribari-Neto, F., Souza, T. C., and Vasconcellos, K. L. P. (2007).
Inference under heteroskedasticity and leveraged data. \emph{Communications in
Statistics - Theory and Methods}, 36(10), 1877-1888.
\doi{10.1080/03610920601126589}

Li, S., Zhang, N., Zhang, X., and Wang, G. (2016). A new
heteroskedasticity-consistent covariance matrix estimator and inference
under heteroskedasticity. \emph{Journal of Statistical Computation and
Simulation}, 87(1), 198-210.
\doi{10.1080/00949655.2016.1198906}
}
